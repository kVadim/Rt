import React, { Component } from 'react';
import './App.css';

class LoginForm extends Component {
    render () {
        return (
            <div>Login</div>
        );
    }
}

export default LoginForm;